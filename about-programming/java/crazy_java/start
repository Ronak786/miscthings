jdk  java standard development tool, install this will also install java run time

安装之后要设置ＰＡＴＨ　以及ｃｌａｓｓｐａｔｈ路径

javac  -d destdir   sourcefile  可以把class文件放到指定的目录里面去

classpath:
	jdk 1.4及以前需要设置，包含dt.jar  tools.jar 以及当前目录
	之后就自动包含这三者了，但是如果明确指定了classpath,还是会依照这个来

	可以在命令行java -classpath  xxx:xxx:xxx来指定这一次的，　win上是；分号


文件名和类名：
	java每个文件可以有多个类，如果没有public类，那么文件名随意，
		如果有那么文件名和public类名一样，所以最多一个文件一个public类，
	建议一个文件一个类，这样清晰，java 必须以类为单位，类外面没有执行性的语句

垃圾回收：
	不使用的变量尽量把引用置为null
	jvm 不同的算法也不一样，精准定位以及全部定位上存在各自不同的缺点

结构化编程　与面向对象编程：
	结构化，Ｃ类系的，是面向功能，也就是函数的，分析设计的时候需要有自定向下的思考过程，
		先考虑主流程，然后细分，最后决定细节，直到最后，反人类做法

面向对象：
	封装，继承，　多态（子类可以附给父类，然后具有使用这个父类时具有子类的特征！！，提高抽象泛用性）

	概念区别：基于对象，这个同样是对象封装，但是不具有继承和多态特性

ＵＭＬ　统一建模语言：用来ｏｏａ　ｏｏｄ　面向对象分析和设计，描述完后用ｏｏｐ面向对象编程完成

类之间的关系：
	a is-a b , 继承关系
	a has-a b, 组装关系
