颜色，基础色和强烈色，后者用于按钮之类的，需要两者对比鲜明一点
字体，应使用sp单位，这样放大字体就会放大sp所占比例，可以整体放大

style 用于一个view  : parent 继承父style
theme  用于一个layout

多屏幕：
    每个文件可以多个版本
    layout-sw500dp 的意思是屏幕的最小边的宽度至少500的时候使用这个文件夹

selector:
    这个是drawable的资源，可以应用于一个view，然后设置不同状态下的行为比如颜色，
        设置activated 或者press状态下的颜色等等
