use this tool:
	kpatch -s srcdir -v vmlinuxfile   patchfile
		to create a hot fix patch using ftrace as a module
	kpatch -k version install xxxxx
	systemctl start kpatch ; every time boot up, will auto load into them
