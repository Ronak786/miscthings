use this tool:
	first compile kpatch from current kernel , then install it
	kpatch -s srcdir(from srcrpm) -v vmlinuxfile(from kernel-debuginfoxxxxx   patchfile
		to create a hot fix patch using ftrace as a module
	systemctl start kpatch ; every time boot up, will auto load into them
