集合的关系：
	自反性，表示（a,a）是属于关系的
	对称 (a,b)  (b,a) 同时属于或不属于
	反对称(a,b) (b,a) 属于关系的唯一可能的条件是a == b
	(a,b) (b,c) ==> (a,c) 传递性

	关系可以并集，交集，差集
	关系的合成，就是(a,b)  (b,c) ==> (a,c)的关系的集合表示

	另外：对于传递关系，有 R^n 属于R,充要条件
	R^n = R^(n-1) * R 这是定义

多元关系：
	各个集合是关系的域，关系中元素的数量是关系的阶
	关系： 表
	行： 记录
	列： 属性
	唯一标记： 主关键字
	组合标记： 复合关键字
	外延： 关系中的记录内容
	内涵： 关系的名字以及属性

闭包：
	所谓就是在当前关系的基础上，加上最少的组合，以达到指定的关系
	自反闭包：
		就是当前关系和所有自反数对的并集
	对称闭包：
		当前关系和当前反过来的关系的并集
	传递闭包：
		首先定义联通关系，就是经过n次路径可以到达的关系，
			= S S^2  S^3。。。的并集
		然后可以证明传递闭包就是联通关系，
		然后还可知任何路径都可以用小于等于元素个数的路径来精简，
		这样最终可以用n个的并集表示传递闭包
		可以On^4

		沃舍尔算法：
			每个矩阵元素的值为 w(ij) || (wik) && w(kj) 对于所有k
			具体见书p423，可以On^3

等价关系：
	自反 对称 传递 都满足
	同余就是等价关系

	等价类: 满足一个等价关系的所有元素的集合，这个
		集合中任意两个元素都满足等价关系
		集合中的每个元素叫做代表元 等价类用[a] 来表示a的等价类
	划分：
		任何划分都构成一种等价关系
		

偏序：自反，反对称，传递
	任意两者都偏序，则全序，

	格：任意两个元素都存在最大下界和最小上界
	极大值，极小值 最大的那个不存在对于他的偏序了的值
	拓扑排序：
		任意有限的偏序集都可以扩展为拓扑排序，就是
		一个全序集合
	良序，首先要是全序，然后每个非空子集都有一个最小元素


图：
	无向图，没有环，两点间至多一条线
	多重图，可以两点间多条线
	有向图，有环，最多一条
	有向多重图，可以多条
	混合图，有向加无向

邻接：
	两个顶点间有一条线
关联：
	边关联到顶点
度：
	一个顶点上连的线的数量
孤立点：
	无线关联
悬挂点：
	只有一条线关联

图中所有度的和，线的数量的两倍，因为一条线恰好对应两个顶点上的各一个度
	因此，无向图的度总和是偶数,同样，有偶数个奇数度的顶点

有向图中：
	分为入度和出度，很形象的，自己想一下
	入度和出度都相等，且等于边的数量
	去掉方向，成为有向图的底无向图

完全图：
	任意顶点间都有线连接的简单图（无向）
圈图：
	一圈的图
轮图：
	圈图再加一点连接所有点
n立方体图：
	n维度立体的顶点代表2的n次方各n位2进制的表示，
	其中相邻的只在一位上不一样

偶图： 
	每条边都连接两个不相交的子集中的一个各一个点，
	这样的点对称为二部划分

	偶图的充要条件是可以用两种颜色把所有相邻的顶点区分开来
		还有充要条件是从一个顶点出发经过奇数条边不能回到自己
	
	完全偶图：
		双方都和对方所有点连接

图可以用邻接矩阵来表示
两个图同构只要能找到一种映射使邻接矩阵一样

判断同构最好利用不变量，比如顶点数，边数，不同顶点的度, 以及利用其联通性
	回路的长度等

	无向图：
		连通图是都联通的图，不联通的最大部分为连通分支

	有向图：
		任意互相连通，则为强连通，
		如果只有底图连通，是弱连通

		强连通分支：
			最大的强连通子图（可以是单个点的）

	
一个邻接矩阵的n次方，其中一项就是两个点之间有长为n的通路的数量

无向图:
欧拉回路：充要条件：每个顶点有偶数度数（一个进，一个出）,且为连通图
	简单的图，并且包含所有边，是回路
	可构造：
		先出一个回路，然后去掉用掉的线，剩下的继续勾回路，然后和之前的拼在一起，
	
欧拉通路 充要条件： 只有两个奇数度的点，其他都是偶数度，（可构造）
	简单图，包含所有边，不是回路

有向图:
	回路： 猜测： 入度等于出度，每个顶点
	通路： 猜测： 有一个顶点出度比入度多一，另一个反过来，其他的相等

哈密顿通路
哈密顿回路： 包含所有点的简单通路或回路
狄拉克定理： 如果n个顶点的图n大于等于3，每个顶点的度至少n/2，那么有哈密顿回路
奥尔鼎炉：n大于等于3，每一对不相邻的顶点的度之和大于等于n，那么有哈密顿回路

格雷码:
	相邻的编码只有一位不一样，可以用少一位的编码先这样排好，然后
	把第三位加上，复制一份，一份加0，一份加一，然后对应相连，
	这样，再找到哈密顿回路，就成了

最短路径：
	迪克斯特拉算法，每一步都包含当前路径最近的那个点，然后计算所有其他点
		当前的最短路劲，为原来的路径或者从内部某个点连出去的多一的路径中最小的那个
	
可平面图：
	可以片面上没有交叉地画出地图，其中K5， K3，3 都是不可平面图（5点完全图，3对3偶图）

欧拉公式：
	连通平面图上 点数加面数 - 边数 = 2(平面图是指可以平面化地图）

	就如果是可平面的图， if v>=3, e <= 3v - 6
						if not have 3 edge's 回路, e <= 2v -4
						存在度数不超过5的点

	枯拉图斯定理：
		不可平面图 等价于 存在子图 与 K3，3 或 K5 同胚， 
			也就是可以从一张图通过去一条边，加一个点和两条边的方法得到另一张图，
			如此往复，叫做初等细分，这样细分多次的两张图叫同胚

四色定理：
	任意可平面的图可以用四种颜色着色，相邻不同色
can use this to solve problem about:
	how many object you must have at the diff time(so they should be set to diff color)


