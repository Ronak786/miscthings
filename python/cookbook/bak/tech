解压tuple赋值 a,b,c,(d,e,f) = haha  必须是同样的结构，内部元组也可以一个变量直接赋掉

aa,*bb, cc =  dd 这样可以将中间部分提取呈一个list

*:
    传参的时候，表示把列表展开
    函数接受的时候，表示参数里传的多个元素收集呈一个列表（贪婪收集）

收集废弃：
    _ , list:  *_


xx=slice(20,24,2)  这样获得的对象可以传给列表，作为切片使用，
    减少硬编码
    xx.indices(len(xxx)) 用这个切片适配新的长度的序列，返回
        合适的三元组,使用三元组的时候要*,作为展开
