8皇后：
	1,从头上开始，每次遍历一行中的一个，对于每个，深度优先下去，同时
		把当前已经不冲突的那些放到数组中传下去
		(可以用tuple，只要相加就能创造新的）
	2,递归，假设最后一次成功，就返回那个值，
		非最后一行，对于后一行的每一个成功序列（递归表示）
			把自己加进去，返回

注意你的模块名字千万不要和系统的一样，不然你内部引用会引用不到的！！！

对象的继承，实际上就是利用已有的功能，
这些功能组件本身可以很好的工作，框架已经有了，但是你
想利用这些功能，在某些功能点上想要有改动，然后还要改动后
不会影响整体框架，这时候利用继承，子类可以完美赋值给父类使用，
功能点改动就你改的那几个，这样就完美了
