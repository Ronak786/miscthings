bubble:
    教学用，从最后第二个索引开始第一轮循环，
        从第一个到上面那个数开始第二轮，
            每次交换当前和之后那一个如果顺序反的话，即可
select:
    每次选择最大的放到当前范围的末尾，比较和冒泡一样，交换要
        少得多，每轮只有一次

insert:
    每次把当前插入前面已经排序的list，如果部分有序，可以很高效
        适合作为高级排序的小范围优化使用
shell:
    插入排序的间隔从达到小，
    推荐 2^k -1 O(n^(3/2))

merge:
    合并，每次二分，直到只剩下一个数字为止，这个base情况直接返回，每次得到两个
        分组后进行组合,需要一个额外数组

qsort:
    找一个样本数，然后从左到有把两边的数交换如果left > sample && right < sample，
        直到right < left, 然后交换right , smaple_index,因为这时候right指向小的地方，
        sample本来在小的地方

        交换的时候，可以从左开始，只要不超过右下标同时又不大过中间数，就下标加一，右边同样，
            全部停下后比较时候互相超过，过的话循环停止，否则交换，继续
        中间数选择 shuffle 后的第一个，或者medium three: first, mid, last的中位数
