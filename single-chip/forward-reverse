旋转开关：
	有3个脚的输出，其他两个接地
	3个中1个是按下用的，另外两个：
		不旋转的时候高电平，旋转的时候会出现低电平，
		但是两者的相位不同，可以利用a在低电平的时候b的状态来决定旋转方向

	第一种：(基于：a 比 b 早半个周期开始低电平）
		中断用于刷新，具体数字的增减
		在一个循环检测里实现
		主要基于循环速度绝对大于旋转速度，这样两个开关
			引脚的高低电平输出时间足够检测，不断循环更新
			B引脚的电平，然后在A低电平（循环直到高电平为止）
			记录下B最后的电平，和前面的比较，根据上升还是下降panduan当前是左右旋转。
			这个前提是可以在A低电平刚开始出现的时候就迅速获取的B的初始，
			（其实检测晚了也可以，只要把 11， 00 这另外两种情况也考虑进去就行了）

	第二种：
		从两个都是高电平开始，两者的哪个先跳到低电平不一样，由这个来判断，选择加减数字，
		防抖：对跳变判断两次（就是对跳变后的值判断两次循环，然后才确定跳变）
