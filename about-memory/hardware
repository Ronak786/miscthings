我们使用6个或4个晶体管组成sram，
	一个以及电容组成dram， 
		dram优点是小，密度大，缺点是放电丢失，所以有一个十毫秒级别的刷新频率，
		还有每次读取都要refresh，读写都有一定延时，这个也是频率提高时的考虑点

		一般使用行列形式的地址线通过mmu连到ram上，所有cpu虚拟的地址经过内部
		转换成物理地之后传给mmu，然后同样要映射，转换，再给ram，
		ram通过fsb（前端总线）和mmu连接交流，所以ram频率决定了fsb频率

		sdram，同步ram，就是有时钟信号（mmu发出）控制的操作
		ddr，双倍ram，就是单时钟多信号
