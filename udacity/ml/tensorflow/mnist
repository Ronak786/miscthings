tf.examples.tutorial.mnist input_data 这个是内部默认的测试集合，会自动下载如果不存在
返回对象mnist
	mnist.train.next_batch(number) 获取批量的输入

分析：
	使用激活函数，隐藏层，可以大幅提高准确率，这两个必要
	ｍｎｉｓｔ的收敛很快，所以学习率衰减，滑动模型，正则化误差效果不是很大，
		因为这些优化在都是对于大幅修改起到平滑，减缓作用的
		但是对于准确率收敛慢的情况，这些在随机批量梯度上会有很好的作用

	如果没有正则化值，会有过度拟合的可能
	学习率和滑动平均用于减少波动，快速靠近最优解
